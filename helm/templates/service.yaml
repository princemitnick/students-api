apiVersion: v1
kind: Service
metadata:
  name: {{ include "students-api.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "students-api.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}